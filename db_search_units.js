require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('gov_units').select('kode_unit, nama_unit').ilike('nama_unit', '%Pengabdian%'); console.log('Pengabdian:', data); const { data: d2 } = await supabase.from('gov_units').select('kode_unit, nama_unit').ilike('nama_unit', '%Sertifikasi%'); console.log('Sertifikasi:', d2); } run();  
